NODE_ENV=development
PORT=5000

DATABASE_URL=mongo db database url
FRONT_END_BASE_URL=front end base url 
BCRYPT_SALT=your bcrypt round number

FRONT_END_BASE_URL='http://localhost:5173'
BACK_END_BASE_URL='http://localhost:5000'


JWT_ACCESS_SECRET=""
RESET_PASS_ACCESS_SECRET=""
JWT_REFRESH_SECRET=""
INVITE_SECRET=""
JWT_ACCESS_EXPIRES_IN=""
RESET_PASS_ACCESS_EXPIRES_IN=""
JWT_REFRESH_EXPIRES_IN=""
INVITE_EXPIRES_IN=""

SMTP_USERNAME="chat_app"
SMTP_PASSWORD="xokg eqnb ncuf yceo"

# ── Audio/Video Calling (WebRTC) ──────────────────────────────
# Ring timeout before an unanswered call is marked 'missed'
# (milliseconds; clamped to a 5s minimum).
CALL_RING_TIMEOUT_MS=30000
# Max rows returned by GET /api/call/history (1-500).
CALL_HISTORY_LIMIT=200
